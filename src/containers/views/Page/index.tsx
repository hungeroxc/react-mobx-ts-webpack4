import * as React from 'react'
import {Button} from 'antd'

class Page extends React.Component {
    render() {
        return (
            <div className="page">
                page
                <Button>按钮</Button>
            </div>
        )
    }
}

export default Page
